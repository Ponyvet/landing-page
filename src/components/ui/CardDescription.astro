---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["card-description", className]}>
  <slot />
</div>

<style>
  .card-description {
    color: var(--muted-foreground);
    font-size: var(--text-sm);
  }
</style>
